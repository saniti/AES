<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= appName %></title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="container">
    <header>
      <h1><%= appName %></h1>
      <nav>
        <% if (user) { %>
          <a href="/dashboard" class="btn btn-primary">Dashboard</a>
          <a href="/logout" class="btn btn-secondary">Logout</a>
        <% } else { %>
          <a href="/login" class="btn btn-primary">Login</a>
        <% } %>
      </nav>
    </header>

    <main class="hero">
      <div class="hero-content">
        <h2>Welcome to <%= appName %></h2>
        <p>A comprehensive equine health and injury tracking system</p>
        
        <% if (demoMode) { %>
          <div class="alert alert-info">
            <strong>Demo Mode:</strong> The application is running in demo mode. OAuth authentication is bypassed.
          </div>
        <% } %>

        <% if (user) { %>
          <div class="user-info">
            <p>Welcome back, <strong><%= user.name %></strong>!</p>
            <a href="/dashboard" class="btn btn-large btn-primary">Go to Dashboard</a>
          </div>
        <% } else { %>
          <div class="cta">
            <a href="/login" class="btn btn-large btn-primary">Get Started</a>
          </div>
        <% } %>
      </div>
    </main>

    <footer>
      <p>&copy; 2025 <%= appName %>. All rights reserved.</p>
    </footer>
  </div>
</body>
</html>
